# galaxy-bank
Bank that handles a currency called Quanta in a galaxy far away

# Setting Environment
Use `source ./set-env.sh` to set your environment variables and `source ./unset-env.sh` to unset them.